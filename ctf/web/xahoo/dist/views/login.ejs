<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login to Xahoo</title>
    <style>
        * {box-sizing: border-box;}
        body {
            margin: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: system-ui, -apple-system, sans-serif;
            background: #e8e8e8;
            padding: 1rem;
        }
        .card {
            width: 100%;
            max-width: 400px;
            background: #fff;
            border: 1px solid #ddd;
            border-radius: 10px;
            padding: 2.5rem 2.25rem;
        }
        .logo {
            text-align: center;
            margin-bottom: 1.5rem;
        }
        .logo .mark {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            background: #555;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 1.25rem;
            color: #fff;
            margin: 0 auto 0.75rem;
        }
        h1 {
            font-size: 1.4rem;
            margin: 0;
            color: #222;
        }
        .subtitle {
            text-align: center;
            color: #888;
            font-size: 0.9rem;
            margin: 0 0 1.5rem;
        }
        input {
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 0.95rem;
            font-family: inherit;
            padding: 0.75rem 0.9rem;
            margin-top: 0.9rem;
            outline: none;
            background: #fff;
            color: #222;
        }
        input:focus {
            border-color: #888;
        }
        input[type="submit"] {
            margin-top: 1.5rem;
            background: #222;
            color: #fff;
            border: none;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            padding: 0.85rem;
            border-radius: 6px;
        }
        input[type="submit"]:hover {
            background: #444;
        }
        .alt {
            text-align: center;
            margin: 1.25rem 0 0;
            font-size: 0.88rem;
            color: #888;
        }
        .alt a {
            color: #222;
            font-weight: 600;
            text-decoration: none;
        }
        .alt a:hover {text-decoration: underline;}
        .error {
            margin: 1rem 0 0;
            padding: 0.7rem 1rem;
            background: #fdf0f0;
            border: 1px solid #f0c9c9;
            color: #a33;
            border-radius: 6px;
            font-size: 0.85rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="logo">
            <div class="mark">X</div>
            <h1>Xahoo</h1>
        </div>
        <p class="subtitle">Sign in to your inbox</p>
        <form action="" method="POST">
            <input type="email" name="email" placeholder="Your email" required autocomplete="off">
            <input type="password" name="password" placeholder="Your password" required autocomplete="off">
            <input type="submit" value="Sign in">
        </form>
        <% if (messages.error) { %>
            <p class="error"><%= messages.error %></p>
        <% } %>
        <p class="alt"><a href="/loginwithkey">Login with key instead</a></p>
    </div>
</body>
</html>
